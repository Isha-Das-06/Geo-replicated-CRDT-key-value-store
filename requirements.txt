# No third-party dependencies — pure Python stdlib only.
# This keeps the project portable and runnable anywhere Python 3.8+ is installed.
#
# Optional: install these for enhanced output (not required)
# tabulate>=0.9.0    # prettier tables
# colorama>=0.4.6   # colored terminal output on Windows
